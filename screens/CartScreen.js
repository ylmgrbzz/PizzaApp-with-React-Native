import React from "react";
import { SafeAreaView, Text } from "react-native";

const CartScreen = () => {
  return (
    <SafeAreaView>
      <Text></Text>
    </SafeAreaView>
  );
};

export default CartScreen;
